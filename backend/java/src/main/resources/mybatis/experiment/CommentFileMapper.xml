<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.experiment.mapper.CommentFileMapper">

    <insert id="insert" parameterType="com.ruoyi.experiment.pojo.entity.CommentFile">
        insert into comment_file (comment_id, file_name, file_path, file_type, file_size,create_time,update_time)
        values (#{commentId}, #{fileName}, #{filePath}, #{fileType}, #{fileSize},#{createTime},#{updateTime})
    </insert>
    <insert id="insertBatch" parameterType="java.util.List">
        insert into comment_file (comment_id, file_name, file_path, file_type, file_size,create_time,update_time)
        values
        <foreach collection="commentFileList" item="item" index="index" separator=",">
            (#{item.commentId}, #{item.fileName}, #{item.filePath}, #{item.fileType}, #{item.fileSize},#{item.createTime},#{item.updateTime})
        </foreach>
    </insert>
</mapper>