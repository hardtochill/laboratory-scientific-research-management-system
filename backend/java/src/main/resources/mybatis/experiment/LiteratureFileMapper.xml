<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.experiment.mapper.LiteratureFileMapper">

    <insert id="insert">
        insert into literature_file (literature_id, file_name, file_path, file_type, file_size, upload_user_id, upload_time, create_time, update_time)
        VALUES
        (#{literatureId},#{fileName}, #{filePath}, #{fileType}, #{fileSize},  #{uploadUserId}, #{uploadTime}, #{createTime}, #{updateTime})
    </insert>
</mapper>